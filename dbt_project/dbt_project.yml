name: sierra_safety
version: '1.0.0'
config-version: 2

profile: sierra_safety

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  sierra_safety:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: incremental
      +incremental_strategy: merge
